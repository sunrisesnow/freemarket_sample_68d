.wrapper 
  = render "shared/header"
  .show-contents
    = render "shared/show-contents"
    .content
      .likes__title
        お気に入り！一覧
      - if @items.present?
        = render "shared/mypage-items-show"
      - else
        .user__liked__not__items
          %i.fas.fa-box.fa-6x
          %p お気に入り！がありません
  = render "shared/footer-image"
  = render "shared/footer"
  = render "shared/saler-btn"