%h2 商品送付先住所登録
= form_for @address , builder: FormHelper::FormWithErrorMessageBuilder do |f|
  -# 
    FormHelperをapp/helpers/form_helperに指定することでエラー内容を各要素のフォームのしたに出るように設定しました。

  -# 
    従来のdeviseのFormHelperを適用し画面上部にエラー内容を表示したい場合FormHelperのオプションを消去し= render "shared/error_messages"を画面上部に書き込んでください
  .field
    = f.label :fullname
    %br/
    = f.text_field :last_name, autocomplete: "last_name", placeholder: "例）山田", autofocus: true
    = f.text_field :first_name, autocomplete: "first_name", placeholder: "例）太郎"
  .field
    = f.label :fullname_kana
    %br/
    = f.text_field :last_name_kana,autocomplete: "last_name_kana", placeholder: "例）ヤマダ"
    = f.text_field :first_name_kana, autocomplete: "first_name_kana", placeholder: "例）タロウ"
  .field
    = f.label :postal_code
    %br/
    = f.text_field :postal_code, autocomplete: "postal_code", placeholder: "例）1234567"
  .field
    = f.label  :prefectures
    %br/
    = f.select :prefectures, Address.prefectures.keys, {prompt: '------'}, class: "select"
  .field
    = f.label :municipality
    %br/
    = f.text_field :municipality, autocomplete: "municipality", placeholder: "例）横浜市緑区"
  .field
    = f.label :address
    %br/
    = f.text_field :address, autocomplete: "address", placeholder: "例）青山 1-1-1"
  .field
    = f.label :building
    %br/
    = f.text_field :building, placeholder: "例）草薙ビル 103"
  .field
    = f.label :phone_number
    %br/
    = f.text_field :phone_number, autocomplete: "phone_number", placeholder: "例）09012345678"
  .actions
    = f.submit "Sign up"
= render "devise/shared/links"