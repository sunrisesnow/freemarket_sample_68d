.wrapper 
  = render "shared/header"
  .show-contents
    = render "shared/show-contents"
    .content
      .likes__title
        下書き一覧
      - if @items.present?
        = render "shared/mypage-items-edit"
      - else
        .user__liked__not__items
          %i.fas.fa-box.fa-6x
          %p 下書きがありません
  = render "shared/footer-image"
  = render "shared/footer"
  = render "shared/saler-btn"