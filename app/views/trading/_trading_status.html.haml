
.content__trading__nav
  %i.fas.fa-truck-moving
  = trading_status_check(item ,saler_user,  buyer_user)
- if item.trading_status_id == 2 && buyer_user == current_user
  = render partial: 'evaluation-form', locals: { item: item, evaluation: evaluation}
- elsif item.trading_status_id == 3 && saler_user == current_user
  = render partial: 'evaluation-form', locals: { item: item, evaluation: evaluation}
- else
  .content__trading__box
    = transaction_button(item ,saler_user, buyer_user)
- if item.trading_status_id == 1
  .content__trading__cancel__box
    = link_to "取引キャンセル申請を送る", cancel_item_trading_path(item, current_user), method: :patch, class: "content__trading__cancel__box__btn", remote: true, data: { confirm: '取引キャンセル申請してよろしいですか?',cancel: 'やめる',commit: '申請する', disable_with: "処理中..." }, title: '申請確認'
    %p
      ※商品発送後のお取引キャンセルはいたしかねますので、お取引キャンセルしたい場合は、速やかに取引キャンセル申請を送ってください
